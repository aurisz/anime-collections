import { css } from '@emotion/react'

const styles = {
  formWrapper: css`
    display: flex;
    flex-direction: column;
  `,
  inputWrapper: css`
    margin-bottom: 1rem;
  `
}

export default styles
