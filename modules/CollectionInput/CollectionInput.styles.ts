import { css } from '@emotion/react'

const styles = {
  formWrapper: css`
    display: flex;
    flex-direction: column;

    p {
      margin-top: 0;
      color: #f31260
    }
  `,
  inputWrapper: css`
    margin-bottom: 1rem;
  `
}

export default styles
