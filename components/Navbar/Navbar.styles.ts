import { css } from '@emotion/react'

const styles = {
  container: css({
    display: 'flex',
    justifyContent: 'space-between',
  }),
  navList: css({
    display: 'flex',
    flexWrap: 'wrap',
    listStyleType: 'none',
    gap: '1rem',
  })
}

export default styles;
