import { css } from '@emotion/react'

const styles = {
  container: css`
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem;

    h1 {
      text-align: center;
    }
  `
}

export default styles;
