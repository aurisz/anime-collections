import { css } from '@emotion/react'

const styles = {
  gridItem: css`
    cursor: pointer;

    span {
      display: block;
    }

    img {
      object-fit: fill;
      border-radius: 8px;
    }
  `
}

export default styles
