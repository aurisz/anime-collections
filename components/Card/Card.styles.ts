import { css } from '@emotion/react'

const styles = {
  card: css`
    cursor: pointer;

    img {
      border-radius: 8px;
    }

    p {
      text-align: center;
    }
  `
}

export default styles
